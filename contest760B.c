#include<stdio.h>
#include<string.h>
int main(void){
    char b[100];
    int m, n;
    printf("Enter the number of test cases: ");
    scanf("%d", &n);
    while (n--)
    {
        printf("Enter the number of characters: ");
        scanf("%d", &m);
        for (int i = 0; i < m; i++)
        {
            scanf("%c", &b[i]);
        }
        
        
    }


}